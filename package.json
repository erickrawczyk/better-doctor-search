{
  "name": "better-doctor-search",
  "version": "1.0.0",
  "description": "BetterDoctor API Search Engine",
  "main": "server.js",
  "scripts": {
    "test": "mocha ./**/*.unit.js && cd browser && CI=true react-scripts test --env=jsdom && cd -",
    "start": "node server.js",
    "client": "cd browser && react-scripts start",
    "build": "cd browser && react-scripts build",
    "postinstall": "cd browser && npm install"
  },
  "author": "Eric Krawczyk",
  "license": "ISC",
  "dependencies": {
    "express": "^4.15.4",
    "lodash": "^4.17.4",
    "node-fetch": "^1.7.3"
  },
  "devDependencies": {
    "chai": "^4.1.2",
    "mocha": "^3.5.3"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/erickrawczyk/better-doctor-search.git"
  }
}
